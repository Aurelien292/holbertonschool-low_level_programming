#include <stdio.h>
/**
 *main - 5 alpha
 *
 *Description: prints to the standard *out *put.
 *
 *Return: Always 0 (Success)
 */

int main(void)
{
int Num;
for (Num = 0; Num <= 10; Num ++)
printf("%d",Num)
printf('\n')
return (0);
}
