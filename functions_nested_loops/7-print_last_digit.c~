#include "main.h"

/**
 * print_last_digit - check the code
 *
 * Return: Always 0.
 */
int print_last_digit(int ld)
{
int last_digit = ld % 10;
if (last_digit < 0)
last_digit = -1;
_putchar(last_digit + '0');
return (last_digit);
}
