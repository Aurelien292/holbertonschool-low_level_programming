#include"main.h"

/**
 *_islower - fonction qui tri les minuscules
 *
 *Return: retourn 1 si c'est une minuscule autrement retourne 0
*/

int _islower(Min);
if (Min >= 97 && Min <= 122)
{
return (1);
return (0);
}
