#include "main.h"

/**
 * mul - Vérifie si un caractère est un chiffre.
 * @a: chiffre a calculer
 * @b: chiffre a calculer
 * Return: 1 si c est un chiffre (0-9), 0 sinon.
 */

int mul(int a, int b)
{
c = a * b;
return (0);
}
