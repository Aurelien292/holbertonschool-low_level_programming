#include "main.h"
/*
 *main - fichier
 *_isupper- fonction
 *description : voit si c'est une majuscule ou non
 */

int _isupper(int c)
{
if (c >= 65 && c <= 90)
{
return (1);
}
else
{
return (0);
}
}
