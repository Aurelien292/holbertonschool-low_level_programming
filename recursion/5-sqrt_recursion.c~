#include "main.h"

/**
 * _sqrt_recursion - Calcule la racine carrée naturelle d'un nombre.
 * @n: Le nombre dont on veut calculer la racine carrée.
 * Description:
 * Cette fonction calcule la racine carrée naturelle du nombre `n` de manière
 * récursive. Elle commence par tester un premier nombre (`r = 0`) et vérifie
 * si le carré de `r` est égal à `n`. Si c'est le cas, la fonction
 * retourne `r`
 * comme racine carrée de `n`.
 * Si le carré de `r` dépasse `n` ou si `r` devient inférieur à 1,
 * la fonction
 * retourne -1 pour indiquer qu'il n'y a pas de racine carrée
 * entière pour `n`.
 * Si `r` n'est ni la racine carrée ni hors limite, la fonction s'appelle
 * récursivement avec `r + 1` pour essayer un autre nombre.
 * Retour:
 * - La racine carrée naturelle de `n` si elle existe.
 * - -1 si `n` n'a pas de racine carrée entière.
 */
int _sqrt_recursion(int n)
{
int r;
if (r * r == n)
{
return (r);
}
if (r * r > n || r * r < 1)
{
return (-1);
}
_sqrt_recursion(n, r + 1);
}
